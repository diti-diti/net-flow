name: Docker Security Tests

on:
  push:
    branches:
      - main

jobs:
  security_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker-compose build

      - name: Run Trivy vulnerability scan for InfluxDB
        run: docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy --cache-dir /root/.cache/trivy --severity HIGH,CRITICAL --no-progress influxdb:2.0.10

      - name: Run Trivy vulnerability scan for Grafana
        run: docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy --cache-dir /root/.cache/trivy --severity HIGH,CRITICAL --no-progress grafana/grafana-oss:8.0.6

      - name: Install Checkov
        run: pip install checkov

      - name: Run Checkov
        run: checkov --dockerfile telegraf/Dockerfile
